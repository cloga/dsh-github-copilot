# Copilot model roles — retired historical direction

Superseded by user-requested retirement in #158 (alpha.34 candidate). Do not implement or restore this card, planner/executor selectors, or dedicated-session action. Ordinary Sessions and Core-owned subagents are the intended experience; the plugin retains only existing-history compatibility. The original direction below is historical context, not current authorization.

Mode: Operate. Local extension of the established Settings → Models account surface. The user approved the two-model card and dedicated new-session action; no new visual world or concept tournament applies.

## Direction contract

THESIS: Make actual planner/executor ownership explicit without confusing a model authorization list with a fixed execution policy.

OWN-WORLD: Inherit the existing Copilot settings card: system typography, currentColor-derived neutral surfaces, restrained border, native labeled controls, single-column layout. Follow the user's current light/dark theme. No new font, palette, illustration or brand.

STORY: Enable intentionally, choose two advertised account models, save a revision-checked configuration, select an existing workspace, then create one dedicated session. Show unavailable and uncertain states instead of substituting a model or repeating creation.

FIRST VIEWPORT: Heading and concise scope statement, opt-in checkbox, planning/execution selects, acceptance note, workspace select, wrapping save/create actions, live status and explicit reload. Desktop and narrow mobile must remain legible without horizontal overflow.

FORM: Precisely approved local settings-card extension, not a new page. No seed/comp is applicable. Isolated fixture chrome is not production UI.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance. For this incumbent extension the existing source is the design authority; no global DESIGN.md rewrite is in scope. Screenshots are labeled synthetic-fixture evidence, not live installation.
