# Published bundle layer: `dsh plugin add dsh-web-search-provider` applies
# this patch. Rows reference the package by name so Node resolution finds
# the installed entry point; configuration defaults live in the settings
# schema and are intentionally not restated here.
- insert:
    - id: web-search-provider
      name: dsh-web-search-provider
