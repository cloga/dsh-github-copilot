name: CI

on:
  workflow_call:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
        dsh:
          - release: 0.1.1-rc.2
            repository: cloga/deepseek-harness
            commit: a772dbbde82780bff2b9394427e9f0a24cafa1d5
          - release: 0.1.2-rc.1
            repository: deepseek-ai/deepseek-harness
            commit: a66e4702047846cdaa10c66c9d3df3951f5ea70d
          - release: 0.1.3-alpha.1
            repository: deepseek-ai/deepseek-harness
            commit: d347e703908d0406b7a7ef80e3a0e594d86b2215
          - release: 0.1.5-alpha.1
            repository: deepseek-ai/deepseek-harness
            commit: 5dda764ed3aa172535a7967b06ff95d9cbfe536a
          - release: 0.1.5-alpha.2
            repository: deepseek-ai/deepseek-harness
            commit: b2e3b2a0125854567a4a5fcba75782e42fe84901
          - release: 0.1.5-rc.1
            repository: deepseek-ai/deepseek-harness
            commit: 183f08e9c6dde7e36cd2318eaee70b0da08fb35e
          - release: 0.1.5-rc.2
            repository: deepseek-ai/deepseek-harness
            commit: fb2c4b9e698e30edb738bca4cf0618587db7d203
          - release: 0.1.6-alpha.1
            repository: deepseek-ai/deepseek-harness
            commit: 0a15e36e7f82b6ed45af6fa9759f29b40dcd965d
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/checkout@v6
        with:
          repository: ${{ matrix.dsh.repository }}
          ref: ${{ matrix.dsh.commit }}
          path: dsh-upstream
          persist-credentials: false
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          package-manager-cache: false
      - run: pnpm install --frozen-lockfile
      - name: Install Core pi-ai closure
        working-directory: dsh-upstream
        run: pnpm install --frozen-lockfile --filter '@deepseek-ai/dsh-llm-pi-ai...'
      - name: Install optional Chat verification closure
        if: matrix.dsh.release != '0.1.1-rc.2'
        working-directory: dsh-upstream
        run: pnpm install --frozen-lockfile --filter '@deepseek-ai/dsh-client-ui-chat...'
      - run: pnpm verify:upstream -- dsh-upstream
      - name: Verify reasoning presentation with real Core services
        if: matrix.dsh.release != '0.1.1-rc.2'
        run: pnpm verify:reasoning-ui -- dsh-upstream
      - name: Verify real Core config integration
        run: pnpm verify:controlled-core -- dsh-upstream
      - name: Install tagged Session and Remote verification closure
        if: matrix.dsh.release == '0.1.5-alpha.1' || matrix.dsh.release == '0.1.5-alpha.2' || matrix.dsh.release == '0.1.5-rc.1' || matrix.dsh.release == '0.1.5-rc.2' || matrix.dsh.release == '0.1.6-alpha.1'
        working-directory: dsh-upstream
        run: pnpm install --frozen-lockfile --filter '@deepseek-ai/dsh-api-session-controller...'
      - name: Install tagged search verification closure
        if: matrix.dsh.release != '0.1.1-rc.2'
        working-directory: dsh-upstream
        run: pnpm install --frozen-lockfile --filter '@deepseek-ai/dsh-tool-web...' --filter '@deepseek-ai/dsh-web-search-deepseek...'
      - name: Prepare unchanged alpha tagged-source runtime verification
        if: matrix.dsh.release == '0.1.3-alpha.1' || matrix.dsh.release == '0.1.5-alpha.1' || matrix.dsh.release == '0.1.5-alpha.2' || matrix.dsh.release == '0.1.5-rc.1' || matrix.dsh.release == '0.1.5-rc.2' || matrix.dsh.release == '0.1.6-alpha.1'
        shell: bash
        run: |
          node scripts/verify-tagged-core.mjs prepare --root . --core dsh-upstream --target "$RUNNER_TEMP/copilot-tagged-core" --release "${{ matrix.dsh.release }}"
      - name: Exercise actual alpha runtime without building or patching Core
        if: matrix.dsh.release == '0.1.3-alpha.1' || matrix.dsh.release == '0.1.5-alpha.1' || matrix.dsh.release == '0.1.5-alpha.2' || matrix.dsh.release == '0.1.5-rc.1' || matrix.dsh.release == '0.1.5-rc.2' || matrix.dsh.release == '0.1.6-alpha.1'
        shell: bash
        run: |
          node node_modules/vitest/vitest.mjs run --config "$RUNNER_TEMP/copilot-tagged-core/vitest.config.mts"
      - run: pnpm verify
      - run: mkdir artifacts
      - run: pnpm pack --pack-destination artifacts
      - name: Verify packed archive
        shell: bash
        run: |
          version="$(node -p "require('./package.json').version")"
          pnpm verify:tarball -- "artifacts/dsh-github-copilot-$version.tgz"

  published-adapter:
    name: Published adapter (${{ matrix.release }}, ${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        # 0.1.3-alpha.1 has no standalone npm artifacts; the 0.1.5/0.1.6 targets are source targets.
        # Every actual alpha runtime is exercised in the unchanged tagged-source job above.
        release: [0.1.2-rc.1]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          package-manager-cache: false
      - name: Prepare an isolated plugin fixture for published Core APIs
        shell: bash
        run: |
          node scripts/verify-published-core.mjs prepare --root . --target "$RUNNER_TEMP/copilot-published-core" --release "${{ matrix.release }}"
      - name: Install unchanged public dependency artifacts
        shell: bash
        run: |
          cd "$RUNNER_TEMP/copilot-published-core"
          pnpm install --ignore-scripts --strict-peer-dependencies --registry=https://registry.npmjs.org
      - name: Verify actual dependency paths and class identities
        shell: bash
        run: |
          node scripts/verify-published-core.mjs inspect --root "$RUNNER_TEMP/copilot-published-core" --release "${{ matrix.release }}"
      - name: Typecheck and exercise the published native adapter
        shell: bash
        env:
          DSH_PUBLISHED_CORE_RELEASE: ${{ matrix.release }}
        run: |
          cd "$RUNNER_TEMP/copilot-published-core"
          node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit
          node node_modules/typescript/bin/tsc -p tsconfig.tests.json
          node node_modules/vitest/vitest.mjs run tests/preview-provider.spec.ts tests/pi-provider-bridge.spec.ts tests/preview-route.spec.ts tests/account-model-auth.spec.ts tests/published-core.spec.ts tests/single-route.spec.ts tests/search-routing.spec.ts tests/routed-web.spec.ts tests/deepseek-search-fallback.spec.ts

  release-ready:
    name: release-ready
    needs: [verify, published-adapter]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Require all compatibility and package checks
        shell: bash
        env:
          VERIFY_RESULT: ${{ needs.verify.result }}
          PUBLISHED_RESULT: ${{ needs.published-adapter.result }}
        run: |
          test "$VERIFY_RESULT" = success
          test "$PUBLISHED_RESULT" = success
      - uses: actions/checkout@v6
        if: ${{ github.event_name == 'pull_request' }}
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Fetch pull-request base
        if: ${{ github.event_name == 'pull_request' }}
        shell: bash
        run: git fetch --no-tags origin "$GITHUB_BASE_REF:refs/remotes/origin/$GITHUB_BASE_REF"
      - name: Require release metadata for important pull-request changes
        if: ${{ github.event_name == 'pull_request' }}
        run: node scripts/release-policy.mjs --base "origin/${{ github.base_ref }}"

  release:
    needs: [release-ready]
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.release-ready.result == 'success' }}
    permissions:
      contents: write
      id-token: write
    uses: ./.github/workflows/release.yml
